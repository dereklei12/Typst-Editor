Typst Document Example

This is an example of bold text and italic text.

Second-level Heading

You can use slash commands to quickly insert formatting:

Type `/bold` to insert bold text

Type `/italic` to insert italic text

Type `/heading` to insert a heading

This is centered text

/