#set text(font: ((name: "Arial", covers: "latin-in-cjk"), "SimSun"))
= Typst文档示例
这是一个*粗体Bold*文本和_斜体Italic_文本的例子。

== 二级标题
你可以使用slash命令来快速插入格式：
- 输入 `/bold` 来插入粗体
- 输入 `/italic` 来插入斜体 如何处理svg的渲染是一个好问题。后面我要根据 
- 输入 `/heading` 来插入
- 如果是这个服务器的话，速度还是很快的。

#align(center)[#underline[这是居中的文本 如果不是这个服务器，速度减慢很多。]]

#align(center)[#underline[整体的内容是这样的，所以他的预览和编译的速度还是很快的。如果你稍微的慢了一些他的速度就跟不上的话。]]

#align(center)[]

上下服务器的配置，很好的保证。

= Typst文档示例
这是一个*粗体Bold*文本和_斜体Italic_文本的例子。

== 二级标题
你可以使用slash命令来快速插入格式：
- 输入 `/bold` 来插入粗体
- 输入 `/italic` 来插入斜体 如何处理svg的渲染是一个好问题。后面我要根据 
- 输入 `/heading` 来插入
- 如果是这个服务器的话，速度还是很快的。

#align(center)[#underline[这是居中的文本 如果不是这个服务器，速度减慢很多。]]

#align(center)[#underline[整体的内容是这样的，所以他的预览和编译的速度还是很快的。如果你稍微的慢了一些他的速度就跟不上的话。]]

#align(center)[]

上下服务器的配置，很好的保证。

= Typst文档示例
这是一个*粗体Bold*文本和_斜体Italic_文本的例子。

== 二级标题
你可以使用slash命令来快速插入格式：
- 输入 `/bold` 来插入粗体
- 输入 `/italic` 来插入斜体 如何处理svg的渲染是一个好问题。后面我要根据 
- 输入 `/heading` 来插入
- 如果是这个服务器的话，速度还是很快的。

#align(center)[#underline[这是居中的文本 如果不是这个服务器，速度减慢很多。]]

#align(center)[#underline[整体的内容是这样的，所以他的预览和编译的速度还是很快的。如果你稍微的慢了一些他的速度就跟不上的话。]]

#align(center)[]

上下服务器的配置，很好的保证。